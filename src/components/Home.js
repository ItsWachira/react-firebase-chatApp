import React from 'react'

function home() {
  return (
    <div>home</div>
  )
}

export default home

  //   useEffect(() => {

{/* 
           <SendMessage scroll={scroll} />
            <div ref={scroll}></div> */}

  //           //  const querySnapshot = await usersCollectionRef.orderBy('createdAt').onSnapshot(querySnapshot => {
  //           //   setMessages(querySnapshot.docs.map((doc)=> ({...doc.data(), id: doc.id})))}); 

  //           // const data = await getDocs(usersCollectionRef);
  //           //   setMessages(data.docs.map((doc)=> ({...doc.data(), id: doc.id})));   const data = await db.collection('messages').orderBy('createdAt').get();


  //         // const data = getDocs(usersCollectionRef).onSnapshot(snapshot => {;
  //         // setMessages(data.docs.map((doc)=> ({...doc.data(), id: doc.id})));

  //      getDocs(usersCollectionRef).onSnapshot(snapshot => {;
  //         setMessages(snapshot.docs.map((doc)=> ({...doc.data(), id: doc.id})))});


  // }, []);